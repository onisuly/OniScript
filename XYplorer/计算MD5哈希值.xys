  end <curitem> == "", utf8decode("未选中任何文件！");
  if( <selitems> != """<curitem>""" ) {
   hashlist;
  }
  elseif( filesize() < 30000000 ) {
   showhash;
  }
  else {
   $curitem = "<curitem>";
   text hash(,,3)." - $curitem", , , utf8decode("文件MD5值");
  }